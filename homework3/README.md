**上机实验三：类的构成与对象的创建**

一、实验目的

1、熟悉类的创建与类体的构成。

2、熟悉构造方法的种类、用法

3、熟练使用不同的构造方法对对象进行声明与创建

4、能够通过对象名调用自己的变量。

5、 理解对象的内存模式

 

二、实验内容

（1）创建一个学生类Student，该类中有以下变量：姓名name、性别sex、学号sno、年龄age、所属系dep。

暂时不考虑类中的成员方法

创建一个含两个参数的构造方法，对姓名和学号进行初始化

创建一个含三个参数的构造方法，对姓名、性别和学号进行初始化

创建一个含四个参数的构造方法，对姓名、性别、学号和年龄进行初始化

创建一个含五个参数的构造方法，对所有变量进行初始化

 

创建一个主类A，在该类的主方法里分别用上述四种方法创建以下Student类的对象：

s1：张三，202201

s2：李四，男，202202

s3：王五，女，202203，19

s4：周六，女，202204，20，信息管理与信息系统

写输出语句输出这四个对象的所有变量值。

① 将这两个类放在同一个java文件中，编译并运行它

② 将这两个类分别放在不同的文件中，编译并运行它  10分

 

没有将这两个类放在同一个java文件中，编译并运行它  10

将student和A放在同一个java文件中时，文件的名字要和主类即类A的名字相同，你命名为student1所以会报错 5

B1的运行结果未截图  5

未输出对象的所有变量值，如张三的性别，所在系 5

将两个类分别放在不同的文件中时要先编译Student类 5

 

（2）创建一个主类B，在该类的主方法中使用没有参数的构造方法创建一个对象st1，编译并运行它，是否报错？错误原因是什么？如何改正才能使得可以使用无参数的构造方法创建对象？

按上题的改进方法修改student类，并在所属系dep定义的时候赋上初值“信息管理与信息系统”。

 

继续在B类的主方法中使用没有参数的构造方法创建一个Student类的对象st1，

输出这st1的所有变量值。

 

通过“.”运算符操作st1的变量，给这些变量赋如下值：钱七，男，2020001，22，电子商务。

创建一个对象st2，通过构造方法为其赋如下初始值：赵八，女，2020002，20 

 

输出这两个对象的所有变量值。

 

未回答第一个问题  5

没有在所属系dep定义的时候赋上初值“信息管理与信息系统” 5

类B截图不完整  5

题目要求在dep定义的时候赋上初值“信息管理与信息系统”，你在没有参数的构造方法里赋初值，导致赵八的所属系为空 5

未通过构造方法为st2赋初值 10

st1,st2是对象，不是类，你是创建了两个类；未通过“.”运算符操作st1的变量，给这些变量赋值  10

题目要求在B类的主方法中使用没有参数的构造方法创建一个Student类的对象st1，输出st1的所有变量值，但是你输出的对象的引用。  5

未输出这两个对象的所有变量值。  5

 

 

（3）自行设计一个类，这个类中应至少包含三个成员变量与两个构造方法，在该类的主方法中分别用这两种构造方法创建对象，输出这两个对象的变量值。

 

未使用两种构造方法创建对象  -5
