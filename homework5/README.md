一、实验目的

1、熟悉四种访问权限。

2、熟悉继承与多态的使用方法，会应用子类对象的向上转型。

3、抽象类的应用

4、接口的定义与使用

 

二、实验要求

（1）代码必须放在以自己名字和学号命名的文件夹中，并在控制台上进行编译和运行；

（2）源代码与运行结果都必须上传。

 

三、实验内容

1、建立一个交通工具类Transport，该类中有车轮数wheel、限载人数number、颜色color三个私有的成员变量 3；有构造方法 2根据参数值初始化成员变量；有成员方法run 1在屏幕输出“我在路上跑”；成员方法information 1在屏幕输出该交通工具的车轮数、限载人数与颜色。

编写小汽车类Car继承Transport类 1，利用构造方法 2将车轮数初始化为4，限载人数初始化为5人，颜色根据输入的参数来确定。Car类有成员方法run 2（重写），在屏幕输出“我在路上跑”，“我是汽车，我必须行驶在机动车道上”。成员方法information 1（继承）在屏幕输出该交通工具的车轮数、限载人数与颜色。

编写自行车类Bike继承Transport类 1，利用构造方法 2将车轮数初始化为2，限载人数初始化为1人，颜色根据输入的参数来确定。Bike类有成员方法run 2，在屏幕输出“我在路上跑”，“我是自行车，我必须行驶在非机动车道上”。成员方法information 1在屏幕输出该交通工具的车轮数、限载人数与颜色。

在主类Test中创建Transport类对象t1 2，它有6个轮子，限载16人，蓝色。调用其run与information方法 2。创建Car类对象c1  2，它是红色 。调用其run与information方法 2。创建Bike类对象b1 2，它是黑色。调用其run与information方法  2。

​                               

 

2. 定义一个抽象类Person 1，它包含私有的成员变量name 1，name通过构造方法来初始化 2。它还包含用来获取姓名的普通成员方法getName() 2和用来描述人的抽象方法getDescription() 2。

从Person类派生出Employee类 1和Student类 1，Employee类拥有新的私有成员变量department 1, Student类拥有新的私有的成员变量school 1。这两个类都用getDescription方法来进行描述。其中Employee类的描述是输出“在xx部门工作的职工”2；Student类的描述是输出“在xx学院上学的学生”2。

创建一个主类PersonDemo，主类中有introduce方法，该方法的参数是一个Person类的对象 2，输出是这个人的姓名和描述 2。主类中有main方法，该方法有如下内容：

① 首先声明一个Person类的变量p 1，随后实例化一个Employee类的对象Tom 1,他的姓名为Tom，部门为销售部.将Tom上转型为p 1, 调用getDescription()方法 1。

② 通过实例化一个personDemo类的对象来调用introduce方法 2，并通过多态实现输出职工（王五，信息部）2以及学生（李明，管理学院）2的姓名和描述。

 

 

3. 定义接口IShape1 2，它包含area方法 2；定义接口IShape2 2，它包含perimeter方法  2；定义圆Cycle类，该类中包含成员变量半径r 2；定义正方形Square类，该类中包含成员变量边长l 2；二者都实现了IShape1和IShape2接口 4，在类中分别用area方法来计算面积 2，用perimeter方法来计算周长 2。

定义主类ShapeDemo，main方法中实例化了一个Cycle对象 1与一个Square对象 1，并输出了它们的半径（或边长）、面积与周长 6。

 

 

半径与边长未初始化  26

未输出圆的周长  28
