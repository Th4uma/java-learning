1. 使用嵌套 for 循环打印如下直角三角形图案（5 行）。

要求：
 外层循环控制行数，内层循环控制每一行输出的字符个数。

——

1. 定义一个 Student 类，包含以下内容：
    成员变量：

- name
- score（成绩）

静态成员变量：

- school（学校，默认值为“XX中学”）

静态方法：

- showSchool()，用于输出学校名称

成员方法：

- showScore()，用于输出学生姓名和成绩

编写测试类，完成以下操作：

- 修改静态成员变量 school
- 调用相关方法

要求：
 体现静态成员属于类、由所有对象共享的特性。

——

1. 定义一个 Shape 接口，包含以下抽象方法：

- getArea()：计算面积
- getPerimeter()：计算周长

定义 Rectangle 类实现 Shape 接口，包含：
 成员变量：

- width（宽）
- height（高）

构造方法：

- 用于初始化 width 和 height

实现接口中的所有抽象方法。

编写测试类，创建 Rectangle 对象并调用相关方法。

要求：
 接口中的方法默认是 public abstract，实现类必须重写全部抽象方法。

——

1. 定义一个 Drawable 接口，包含抽象方法：

- draw()

定义 Square 类实现 Drawable 接口，包含：
 成员变量：

- side（边长）

构造方法：

- 用于初始化 side

实现 draw() 方法，输出：
 “绘制边长为 XX 的正方形”

在 Square 类中额外定义：

- getArea() 方法，用于计算正方形面积

编写测试类，创建 Square 对象并调用相关方法。

要求：
 实现类既需要重写接口方法，又可以拥有自身特有的方法。

——

1. 编写一个 Java 程序，实现以下功能：

- 从键盘输入 10 个整数
- 找出这 10 个整数中的最大值和最小值
- 计算并输出所有整数的平均值
